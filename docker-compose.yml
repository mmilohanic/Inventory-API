services:
    db:
        image: mcr.microsoft.com/mssql/server:2022-latest
        environment:
            ACCEPT_EULA: "Y"
            SA_PASSWORD: "Str0nkP@ss"
        ports:
            - "1433:1433"
        networks:
            - app-network

    api:
        build: .
        ports:
            - "8080:8080"
        environment:
            ASPNETCORE_ENVIRONMENT: "Development"
            ConnectionStrings__DefaultConnection: "Server=db,1433;Database=InventoryDb;User Id=sa;Password=Str0nkP@ss;TrustServerCertificate=True;"
        depends_on:
            - db
        networks:
            - app-network

networks:
    app-network:
        driver: bridge
